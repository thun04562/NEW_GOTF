using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SitFirePoint : MonoBehaviour
{
    /*PlayerMovement thePlayer;
    [SerializeField] Vector2 standPos, sitPos;

    void Start()
    {
        thePlayer = GetComponentInParent<PlayerMovement>();
        standPos = transform.localPosition;
    }

    void Update()
    {
        if (thePlayer.isSitting)
        {
            transform.localPosition = sitPos;
        }

        if (!thePlayer.isSitting)
        {
            transform.localPosition = standPos;
        }
    }*/
}
